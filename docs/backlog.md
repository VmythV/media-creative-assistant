# 开发待办清单（Backlog）

维护规则：新想法先入本清单；立项时在 `todolist.md` 建里程碑并回填状态；完成后标记 ✅ 并注明里程碑。
优先级：★★★ 优先做 / ★★ 排队 / ★ 有价值但不急 / — 触发条件未到。
上游：`phase2-roadmap.md`（北极星与设计原则不变：自然语言优先、IR 唯一契约、能力边界透明、确定性兜底）。

## 一、核心目标深化（让"AI 做视频"更像真的剪辑师）

| # | 事项 | 说明 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| B1 | 成片自检闭环 | 渲染后抽帧回喂视觉模型自查（黑场/响度/时长偏差/重复素材 + 画质/字幕/构图雷同），报告 + 可执行建议——从"执行指令"到"对结果负责"。自动修复拆为 B22 | ★★★ | ✅ M23 |
| B2 | 片段级修订意图 | `edit_clips` 六种确定性局部操作（trim/remove/move/subtitle/transition/replace），秒级完成不经 LLM 重排；位置指代以状态简报片段清单锚定 | ★★★ | ✅ M22 |
| B3 | 对白驱动选段 | "把讲美食的部分剪进来"：转写分段与镜头对齐 + 语义检索选段 | ★★ | 等 M20(b) 真实对白素材验证后 |
| B4 | 多版本方案 | "给我两个版本对比"：一次生成 2-3 个不同结构/节奏方案并排选 | ★ | 待排期 |
| B22 | 自检自动修复 | review issues 编译 fix_ops → apply_clip_ops 一键修复（时长偏长→trim 全片段、黑场/重复→replace）；无 review 先自检，替换失败退回仅修剪，主观问题如实归为手动 | ★★ | ✅ M24 |

## 二、功能扩展（剪辑能力补全）

| # | 事项 | 说明 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| B5 | 变速 | IR v0.6 `clip.speed`（0.25-4.0）+ 渲染 setpts/atempo；edit_clips speed 操作。Resolve 侧原速兜底 + 手动 Retime 提示 | ★★ | ✅ M25 |
| B6 | 旁白编排 | TTS 产物入 IR 旁白轨（时间线对齐 + 配乐自动闪避），"给视频配上解说"一句话可达 | ★★ | 等 TTS Extras 安装后 |
| B7 | 片头片尾/标题卡 | plan.clips kind:title 条目 → plan_to_ir 生成纯色文字视频源当普通 Clip（IR 无感知，Resolve 也可见）；add_title 意图/API | ★★ | ✅ M26 |
| B8 | 调色滤镜预设 | "暖一点""电影感"：ffmpeg eq/curves 预设 | ★ | 待排期 |
| B9 | 画中画/多视频轨 | IR 多视频轨结构性扩展 + overlay 渲染，最重 | ★ | 待排期 |
| B10 | 平台导出预设 | 抖音/B站规格一键（M13+M20 已覆盖大半，剩码率微调） | ★ | 价值待评估 |
| B21 | 竖屏构图优化 | M23 自检真实反馈：横素材竖屏化 blur 填充有效画面占比不足——主体感知裁切（视觉模型定位主体 → crop 跟随）或按画面内容智能选 crop/blur | ★★ | 待排期 |

## 三、用户体验（把已有能力磨亮）

| # | 事项 | 说明 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| B11 | 对话内嵌成片预览 + 实时进度 | 动作卡内嵌 `<video>` 直接播；SSE 进度（"视觉理解 3/6""渲染片段 4/6"）透传到卡片 | ★★★ | ✅ M21 |
| B12 | 发布文案包 | 渲染完成后模型生成标题/简介/话题标签（受限格式），成片+文案一起交付 | ★★ | ✅ M21 |
| B13 | 方案时间线可视化 | 片段列表 → 横向时间线图（块宽=时长、转场标记、字幕气泡） | ★★ | 待排期 |
| B14 | 素材原片预览 | 点缩略图弹窗播放原素材 | ★ | 待排期 |
| B15 | 错误信息人话化 | ffmpeg/API 报错映射为"是什么+怎么办" | ★ | 待排期 |
| B16 | 首次使用引导 | 空库 onboarding：.env 缺 key 检测与指引、三步引导 | ★ | 待排期 |

## 四、工程储备（触发条件未到）

| # | 事项 | 触发条件 | 状态 |
| --- | --- | --- | --- |
| B17 | Ollama 本地兜底 | 断网/欠费场景出现 | — |
| B18 | MCP Server 化 | 出现外部 Agent 消费者 | — |
| B19 | Resolve 内嵌面板（Workflow Integration） | 核心体验稳定后的形态升级 | — |
| B20 | output 目录清理策略 | 磁盘堆积成为问题 | — |

## 推进顺序（2026-07-15 更新）

已完成：M21 = B11+B12（对话预览/进度 + 发布文案包）→ M22 = B2（片段级修订）→ M23 = B1（成片自检）。★★★ 档清空。

后续建议：**M20(b) 真实素材验收随时插队（等用户素材目录）**；功能线按 ★★ 档推进——
B13 时间线可视化 → B21 竖屏构图优化；
B3/B6 各有前置条件（真实对白素材 / TTS Extras），满足后插入。以 M20(b) 的实测发现为准动态调整。
